export namespace entry {
    const main: string[];
    const admin: string[];
}
export const cacheBusting: string;
export namespace externals {
    const jquery: string;
}
export const publicPath: string;
export const watch: string[];
export const devUrl: string;
export namespace translation {
    const domain: string;
    const filename: string;
    const languageDir: string;
    const bugReport: string;
    const translator: string;
    const team: string;
}
